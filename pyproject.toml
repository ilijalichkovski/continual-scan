[project]
name = "continual-scan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=1.21",
    "matplotlib>=3.4",
    "networkx>=2.6",
]
